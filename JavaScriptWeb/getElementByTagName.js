// Quando abrir o live server e entrar no console, não confundir o HTML Collection com Array, mesmo estando dentro
// de colchetes. 

const element = document.getElementsByTagName('h1') // Seleciona o elemento pela sua tag

console.log(element)