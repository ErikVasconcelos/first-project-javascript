// Quando abrir o live server e entrar no console, não confundir o HTML Collection com Array, mesmo estando dentro
// de colchetes. 

const element = document.getElementsByClassName("paragraph") // Seleciona o elemento pela sua class

console.log(element)